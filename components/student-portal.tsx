'use client'

import { useState } from 'react'
import Image from 'next/image'
import { Button } from '@/components/ui/button'
import { academicSummary, announcements, courses, grades, libraryItems, navItems, payments, registrarRequests, schedule, student, type PortalSection } from '@/lib/portal-data'
import { ArrowRight, Bell, BookOpen, CalendarDays, ChartNoAxesColumnIncreasing, Check, ChevronRight, CircleHelp, CreditCard, Download, FileText, LayoutDashboard, Library, LogOut, Menu, PanelLeftClose, Printer, Search, Send, UserRound, X } from 'lucide-react'
import { registrarCurriculums } from '@/lib/curriculum-data'

const icons = { LayoutDashboard, BookOpen, ChartNoAxesColumnIncreasing, CalendarDays, CreditCard, FileText, Library, CircleHelp }

export function StudentPortal() {
  const [signedIn, setSignedIn] = useState(false)
  const [section, setSection] = useState<PortalSection>('Overview')
  const [mobileOpen, setMobileOpen] = useState(false)
  const [email, setEmail] = useState('')
  const [password, setPassword] = useState('')
  const [notice, setNotice] = useState('')

  if (!signedIn) return <SignIn email={email} password={password} setEmail={setEmail} setPassword={setPassword} onSignIn={() => setSignedIn(true)} />

  const go = (next: PortalSection) => { setSection(next); setMobileOpen(false); setNotice('') }
  const signOut = () => { setSignedIn(false); setSection('Overview'); setNotice('') }

  return <div className="min-h-screen bg-muted/40 text-foreground">
    <aside className={`fixed inset-y-0 left-0 z-50 flex w-72 flex-col bg-navy-dark text-primary-foreground transition-transform lg:translate-x-0 ${mobileOpen ? 'translate-x-0' : '-translate-x-full'}`}>
      <div className="flex h-24 items-center gap-3 border-b border-primary-foreground/10 px-7">
        <Image src="/images/cdl-logo.jpg" alt="Colegio De Loreto seal" width={52} height={52} className="h-12 w-auto object-contain" />
        <div><p className="font-serif text-lg font-bold">Colegio De Loreto</p><p className="text-[10px] uppercase tracking-[0.16em] text-gold">Student portal</p></div>
        <button className="ml-auto lg:hidden" aria-label="Close menu" onClick={() => setMobileOpen(false)}><X className="h-5 w-5" /></button>
      </div>
      <div className="px-5 py-6"><p className="mb-3 px-3 text-[10px] font-bold uppercase tracking-[0.2em] text-primary-foreground/45">Campus life</p><nav className="flex flex-col gap-1">{navItems.map(item => { const Icon = icons[item.icon as keyof typeof icons]; return <button key={item.label} onClick={() => go(item.label as PortalSection)} className={`flex items-center gap-3 rounded-lg px-3 py-3 text-left text-sm font-semibold transition-colors ${section === item.label ? 'bg-gold text-navy-dark' : 'text-primary-foreground/70 hover:bg-primary-foreground/10 hover:text-primary-foreground'}`}><Icon className="h-[18px] w-[18px]" />{item.label}{item.label === 'Help & Support' && <span className="ml-auto h-2 w-2 rounded-full bg-gold" />}</button> })}</nav></div>
      <div className="mt-auto border-t border-primary-foreground/10 p-5"><div className="mb-4 flex items-center gap-3 rounded-lg bg-primary-foreground/5 p-3"><div className="flex h-9 w-9 items-center justify-center rounded-full bg-gold font-bold text-navy-dark">MC</div><div className="min-w-0"><p className="truncate text-sm font-semibold">Maria Clara Santos</p><p className="text-xs text-primary-foreground/50">{student.id}</p></div></div><button onClick={signOut} className="flex w-full items-center gap-3 px-3 py-2 text-sm text-primary-foreground/60 hover:text-gold"><LogOut className="h-4 w-4" /> Sign out</button></div>
    </aside>
    {mobileOpen && <button className="fixed inset-0 z-40 bg-navy-dark/50 lg:hidden" aria-label="Close navigation" onClick={() => setMobileOpen(false)} />}
    <main className="lg:pl-72">
      <header className="sticky top-0 z-30 flex h-20 items-center justify-between border-b border-border bg-background/90 px-5 backdrop-blur md:px-8"><div className="flex items-center gap-3"><button className="rounded-lg p-2 hover:bg-muted lg:hidden" aria-label="Open menu" onClick={() => setMobileOpen(true)}><Menu className="h-5 w-5" /></button><div><p className="text-xs font-semibold uppercase tracking-[0.16em] text-muted-foreground">Student portal / <span className="text-navy">{section}</span></p><h1 className="font-serif text-xl font-bold text-navy md:text-2xl">{section}</h1></div></div><div className="flex items-center gap-3"><button onClick={() => setNotice('You are all caught up.')} aria-label="Notifications" className="relative rounded-lg p-2.5 text-navy hover:bg-accent"><Bell className="h-5 w-5" /><span className="absolute right-2 top-2 h-1.5 w-1.5 rounded-full bg-gold" /></button><div className="hidden h-8 w-px bg-border sm:block" /><div className="hidden text-right sm:block"><p className="text-sm font-semibold">{student.firstName}</p><p className="text-xs text-muted-foreground">{student.program.replace('Bachelor of Science in ', 'BS ')}</p></div><div className="flex h-9 w-9 items-center justify-center rounded-full bg-navy text-xs font-bold text-primary-foreground">MC</div></div></header>
      {notice && <div className="mx-5 mt-5 flex items-center gap-2 rounded-lg border border-gold/40 bg-accent px-4 py-3 text-sm text-navy md:mx-8"><Check className="h-4 w-4 text-gold-dark" />{notice}<button className="ml-auto" onClick={() => setNotice('')}><X className="h-4 w-4" /></button></div>}
      <div className="mx-auto max-w-7xl p-5 md:p-8">{section === 'Overview' && <Overview go={go} setNotice={setNotice} />}{section === 'My Academics' && <Academics setNotice={setNotice} />}{section === 'Curriculum' && <StudentCurriculum setNotice={setNotice} />}{section === 'Grades' && <Grades />}{section === 'Schedule' && <Schedule />}{section === 'Payments' && <Payments setNotice={setNotice} />}{section === 'Registrar' && <Registrar setNotice={setNotice} />}{section === 'Library' && <LibraryView setNotice={setNotice} />}{section === 'Help & Support' && <Support setNotice={setNotice} />}</div>
    </main>
  </div>
}

function SignIn({ email, password, setEmail, setPassword, onSignIn }: { email: string; password: string; setEmail: (v: string) => void; setPassword: (v: string) => void; onSignIn: () => void }) { return <main className="flex min-h-screen items-center justify-center bg-navy-dark p-5"><div className="w-full max-w-md"><div className="mb-8 text-center"><Image src="/images/cdl-logo.jpg" alt="Colegio De Loreto seal" width={92} height={92} className="mx-auto mb-5 h-24 w-auto object-contain" /><p className="mb-2 text-xs font-bold uppercase tracking-[0.25em] text-gold">Student portal</p><h1 className="font-serif text-3xl font-bold text-primary-foreground">Welcome back.</h1><p className="mt-2 text-sm leading-6 text-primary-foreground/60">Access your academic journey, campus services, and student life.</p></div><div className="rounded-2xl bg-background p-7 shadow-2xl"><form onSubmit={e => { e.preventDefault(); onSignIn() }} className="flex flex-col gap-5"><label className="flex flex-col gap-2 text-sm font-semibold text-navy">Student ID<input value={email} onChange={e => setEmail(e.target.value)} placeholder="e.g. 2026-00142" className="h-11 rounded-lg border border-border bg-background px-3 font-normal outline-none ring-gold transition focus:ring-2" /></label><label className="flex flex-col gap-2 text-sm font-semibold text-navy">Password<input type="password" value={password} onChange={e => setPassword(e.target.value)} placeholder="Enter your password" className="h-11 rounded-lg border border-border bg-background px-3 font-normal outline-none ring-gold transition focus:ring-2" /></label><Button type="submit" className="h-11 bg-gold font-bold text-navy-dark hover:bg-gold-dark">Sign in <ArrowRight className="h-4 w-4" /></Button></form><button onClick={onSignIn} className="mt-5 w-full rounded-lg border border-dashed border-gold/70 bg-accent py-3 text-sm font-semibold text-navy hover:bg-gold/15">Try demo portal without signing in</button><p className="mt-5 text-center text-[11px] leading-5 text-muted-foreground">Demo experience · No real student information is collected</p></div><a href="/" className="mt-6 block text-center text-sm text-primary-foreground/60 hover:text-gold">← Return to Colegio De Loreto website</a></div></main> }

function PageTitle({ eyebrow, title, description, action }: { eyebrow: string; title: string; description: string; action?: React.ReactNode }) { return <div className="mb-7 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="mb-2 text-xs font-bold uppercase tracking-[0.2em] text-gold-dark">{eyebrow}</p><h2 className="font-serif text-3xl font-bold text-navy text-balance">{title}</h2><p className="mt-2 max-w-2xl text-sm leading-6 text-muted-foreground">{description}</p></div>{action}</div> }
function Card({ children, className = '' }: { children: React.ReactNode; className?: string }) { return <section className={`rounded-xl border border-border bg-background p-5 shadow-sm ${className}`}>{children}</section> }
function CardHeading({ title, action }: { title: string; action?: string }) { return <div className="mb-5 flex items-center justify-between"><h3 className="font-serif text-lg font-bold text-navy">{title}</h3>{action && <button className="text-xs font-bold text-navy hover:text-gold-dark">{action} <ChevronRight className="inline h-3 w-3" /></button>}</div> }

function Overview({ go, setNotice }: { go: (s: PortalSection) => void; setNotice: (s: string) => void }) { return <><div className="mb-7 overflow-hidden rounded-2xl bg-navy p-6 text-primary-foreground md:p-8"><div className="flex flex-col justify-between gap-6 md:flex-row md:items-center"><div><p className="mb-2 text-sm text-gold">Tuesday, September 9, 2026</p><h2 className="font-serif text-3xl font-bold md:text-4xl">Good morning, Maria Clara.</h2><p className="mt-3 max-w-xl text-sm leading-6 text-primary-foreground/65">Your academic journey at Colegio De Loreto is off to a great start. Here&apos;s what&apos;s happening this week.</p></div><div className="flex shrink-0 items-center gap-4 rounded-xl bg-primary-foreground/10 px-5 py-4"><div className="flex h-12 w-12 items-center justify-center rounded-full bg-gold text-lg font-bold text-navy-dark">MC</div><div><p className="font-semibold">{student.program}</p><p className="mt-1 text-xs text-primary-foreground/55">{student.year} · {student.id}</p></div></div></div></div><div className="mb-7 grid gap-4 sm:grid-cols-3"><Card className="border-l-4 border-l-gold"><p className="text-xs font-semibold uppercase tracking-wide text-muted-foreground">Academic standing</p><p className="mt-2 text-xl font-bold text-navy">{academicSummary.standing}</p><p className="mt-1 text-xs text-gold-dark">GPA: {academicSummary.gpa}</p></Card><Card><p className="text-xs font-semibold uppercase tracking-wide text-muted-foreground">Current semester</p><p className="mt-2 text-xl font-bold text-navy">{academicSummary.units} units</p><p className="mt-1 text-xs text-muted-foreground">{academicSummary.semester}</p></Card><Card><p className="text-xs font-semibold uppercase tracking-wide text-muted-foreground">Next deadline</p><p className="mt-2 text-xl font-bold text-navy">Sep 20, 2026</p><p className="mt-1 text-xs text-muted-foreground">Tuition payment</p></Card></div><div className="grid gap-5 xl:grid-cols-[1.35fr_1fr]"><Card><CardHeading title="Today&apos;s schedule" action="Full schedule" /><div className="flex flex-col gap-3"><div className="flex items-center gap-4 rounded-lg bg-accent p-4"><div className="w-20 text-xs font-semibold text-navy">8:00 AM</div><div className="h-10 w-1 rounded-full bg-gold" /><div><p className="text-sm font-bold text-navy">AGRI 101 · Introduction to Agricultural Business</p><p className="mt-1 text-xs text-muted-foreground">Room 204 · Prof. Elena Ramos</p></div></div><div className="flex items-center gap-4 rounded-lg border border-border p-4"><div className="w-20 text-xs font-semibold text-navy">1:00 PM</div><div className="h-10 w-1 rounded-full bg-navy/20" /><div><p className="text-sm font-bold text-navy">MATH 101 · Mathematics in the Modern World</p><p className="mt-1 text-xs text-muted-foreground">Room 202 · Prof. Carlo Mendoza</p></div></div></div><button onClick={() => go('Schedule')} className="mt-4 text-xs font-bold text-navy hover:text-gold-dark">View weekly schedule <ArrowRight className="ml-1 inline h-3 w-3" /></button></Card><Card><CardHeading title="Announcements" action="View all" /><div className="flex flex-col divide-y divide-border">{announcements.map(item => <div key={item.title} className="flex gap-3 py-3 first:pt-0 last:pb-0"><div className="w-11 shrink-0 text-center text-[10px] font-bold leading-4 text-gold-dark">{item.date.split(' ')[0]}<br /><span className="text-navy">{item.date.split(' ')[1]}</span></div><div><p className="text-sm font-semibold leading-5 text-navy">{item.title}</p><p className="mt-1 text-[11px] text-muted-foreground">{item.tag}</p></div></div>)}</div></Card></div><Card className="mt-5"><CardHeading title="Quick actions" /><div className="grid gap-3 sm:grid-cols-4"><button onClick={() => go('Grades')} className="flex items-center gap-3 rounded-lg border border-border p-4 text-left hover:border-gold hover:bg-accent"><ChartNoAxesColumnIncreasing className="h-5 w-5 text-gold-dark" /><span className="text-sm font-semibold text-navy">View grades</span></button><button onClick={() => go('Payments')} className="flex items-center gap-3 rounded-lg border border-border p-4 text-left hover:border-gold hover:bg-accent"><CreditCard className="h-5 w-5 text-gold-dark" /><span className="text-sm font-semibold text-navy">Pay tuition</span></button><button onClick={() => go('Registrar')} className="flex items-center gap-3 rounded-lg border border-border p-4 text-left hover:border-gold hover:bg-accent"><FileText className="h-5 w-5 text-gold-dark" /><span className="text-sm font-semibold text-navy">Request document</span></button><button onClick={() => { go('Help & Support'); setNotice('Support center opened.') }} className="flex items-center gap-3 rounded-lg border border-border p-4 text-left hover:border-gold hover:bg-accent"><CircleHelp className="h-5 w-5 text-gold-dark" /><span className="text-sm font-semibold text-navy">Get support</span></button></div></Card></> }

function Academics({ setNotice }: { setNotice: (s: string) => void }) { return <><PageTitle eyebrow="Academic record" title="My Academics" description="View your current enrollment, program information, and registered subjects." action={<Button onClick={() => setNotice('Enrollment assessment downloaded as a demo file.')} className="bg-navy text-primary-foreground hover:bg-navy-light">Download assessment</Button>} /><div className="grid gap-5 md:grid-cols-3"><Card><p className="text-xs uppercase tracking-wide text-muted-foreground">Program</p><p className="mt-2 font-serif text-xl font-bold text-navy">BS Agricultural Business</p><p className="mt-1 text-sm text-muted-foreground">{student.year}</p></Card><Card><p className="text-xs uppercase tracking-wide text-muted-foreground">Semester</p><p className="mt-2 font-serif text-xl font-bold text-navy">1st Semester</p><p className="mt-1 text-sm text-muted-foreground">AY 2026–2027</p></Card><Card><p className="text-xs uppercase tracking-wide text-muted-foreground">Units enrolled</p><p className="mt-2 font-serif text-xl font-bold text-navy">18 units</p><p className="mt-1 text-sm text-muted-foreground">6 subjects</p></Card></div><Card className="mt-5"><CardHeading title="Registered subjects" action="Curriculum guide" /><div className="overflow-x-auto"><table className="w-full min-w-[640px] text-left text-sm"><thead className="border-b border-border text-xs uppercase tracking-wide text-muted-foreground"><tr><th className="pb-3">Course</th><th className="pb-3">Subject</th><th className="pb-3">Instructor</th><th className="pb-3">Units</th></tr></thead><tbody className="divide-y divide-border">{courses.map(c => <tr key={c.code}><td className="py-4 font-bold text-navy">{c.code}</td><td className="py-4">{c.name}</td><td className="py-4 text-muted-foreground">{c.instructor}</td><td className="py-4">{c.units}</td></tr>)}</tbody></table></div></Card></> }

function Grades() { return <><PageTitle eyebrow="Academic record" title="Grades" description="Track your performance for the current semester and previous academic terms." action={<select aria-label="Select academic term" className="h-10 rounded-lg border border-border bg-background px-3 text-sm font-semibold text-navy"><option>1st Semester, AY 2026–2027</option><option>2nd Semester, AY 2025–2026</option></select>} /><div className="mb-5 grid gap-4 sm:grid-cols-3"><Card className="border-l-4 border-l-gold"><p className="text-xs font-semibold uppercase tracking-wide text-muted-foreground">Current GPA</p><div className="mt-2 flex items-end gap-2"><span className="font-serif text-4xl font-bold text-navy">{academicSummary.gpa}</span><span className="mb-1 text-xs font-semibold text-gold-dark">Excellent</span></div><p className="mt-1 text-xs text-muted-foreground">Based on posted grades</p></Card><Card><p className="text-xs font-semibold uppercase tracking-wide text-muted-foreground">Subjects graded</p><p className="mt-2 font-serif text-4xl font-bold text-navy">{grades.length}<span className="ml-1 text-lg text-muted-foreground">/ 6</span></p><p className="mt-1 text-xs text-muted-foreground">18 total units enrolled</p></Card><Card><p className="text-xs font-semibold uppercase tracking-wide text-muted-foreground">Grade release</p><p className="mt-2 text-xl font-bold text-navy">In progress</p><div className="mt-3 h-1.5 overflow-hidden rounded-full bg-muted"><div className="h-full w-4/5 rounded-full bg-gold" /></div><p className="mt-1 text-xs text-muted-foreground">5 of 6 subjects posted</p></Card></div><Card><div className="mb-5 flex flex-col gap-3 border-b border-border pb-5 sm:flex-row sm:items-center sm:justify-between"><div><h3 className="font-serif text-xl font-bold text-navy">Current semester</h3><p className="mt-1 text-xs text-muted-foreground">1st Semester, AY 2026–2027 · Grades update as instructors submit them.</p></div><button className="w-fit rounded-lg border border-navy px-3 py-2 text-xs font-bold text-navy transition-colors hover:bg-accent">Download report</button></div><div className="flex flex-col gap-3">{grades.map(g => <div key={g.code} className="rounded-xl border border-border bg-muted/30 p-4 transition-colors hover:border-gold/60 hover:bg-accent/40"><div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div className="min-w-0"><div className="flex items-center gap-2"><span className="text-[11px] font-bold uppercase tracking-wide text-gold-dark">{g.code}</span><span className="rounded-full bg-accent px-2 py-0.5 text-[10px] font-bold text-navy">Posted</span></div><p className="mt-1 truncate text-sm font-bold text-navy">{g.name}</p><p className="mt-1 text-xs text-muted-foreground">{g.units} units · Current standing</p></div><div className="flex items-center justify-between gap-4 sm:justify-end"><div className="w-28"><div className="mb-1 flex justify-between text-[10px] font-semibold text-muted-foreground"><span>Progress</span><span>{g.progress}%</span></div><div className="h-2 overflow-hidden rounded-full bg-muted"><div className="h-full rounded-full bg-gold" style={{ width: `${g.progress}%` }} /></div></div><span className="flex h-11 w-14 items-center justify-center rounded-lg bg-navy font-serif text-xl font-bold text-primary-foreground">{g.grade}</span></div></div></div>)}</div><div className="mt-6 grid gap-4 border-t border-border pt-5 sm:grid-cols-[1fr_auto] sm:items-center"><div><p className="text-sm font-bold text-navy">Semester GPA</p><p className="mt-1 text-xs text-muted-foreground">Your current weighted average across posted subjects.</p></div><span className="font-serif text-4xl font-bold text-gold-dark">{academicSummary.gpa}</span></div></Card><div className="mt-5 grid gap-4 md:grid-cols-2"><div className="rounded-xl border border-gold/40 bg-accent p-4"><p className="text-sm font-bold text-navy">How grading works</p><p className="mt-1 text-xs leading-5 text-muted-foreground">Grades are computed from quizzes, performance tasks, midterms, and finals. A grade of 1.00 is the highest rating.</p></div><div className="rounded-xl border border-border bg-background p-4"><p className="text-sm font-bold text-navy">Need a correction?</p><p className="mt-1 text-xs leading-5 text-muted-foreground">Contact your instructor or submit a registrar request if you spot an error in your record.</p></div></div></> }

function Schedule() { return <><PageTitle eyebrow="Weekly view" title="Class Schedule" description="Your classes for the week of September 9–13, 2026." action={<Button variant="outline" className="border-navy text-navy">Export schedule</Button>} /><div className="grid gap-4 lg:grid-cols-5">{schedule.map(day => <Card key={day.day} className="p-4"><div className="mb-4 border-b border-border pb-3"><p className="text-sm font-bold text-navy">{day.day}</p><p className="font-serif text-2xl font-bold text-gold-dark">{day.date}</p></div><div className="flex flex-col gap-3">{day.classes.map(c => <div key={c.code + c.time} className="rounded-lg bg-accent p-3"><p className="text-[10px] font-bold text-gold-dark">{c.time}</p><p className="mt-1 text-xs font-bold leading-4 text-navy">{c.code}</p><p className="mt-1 text-[11px] leading-4 text-muted-foreground">{c.name}</p><p className="mt-2 text-[10px] font-semibold text-navy">{c.room}</p></div>)}</div></Card>)}</div></> }

function Payments({ setNotice }: { setNotice: (s: string) => void }) { return <><PageTitle eyebrow="Student finance" title="Payments" description="View your account balance and payment history. This demo does not process real payments." action={<Button onClick={() => setNotice('Payment checkout is available in the production portal.')} className="bg-gold font-bold text-navy-dark hover:bg-gold-dark">Pay now <ArrowRight className="h-4 w-4" /></Button>} /><div className="mb-5 grid gap-4 md:grid-cols-3"><Card className="border-l-4 border-l-gold"><p className="text-xs uppercase tracking-wide text-muted-foreground">Total balance</p><p className="mt-2 font-serif text-3xl font-bold text-navy">₱18,450.00</p><p className="mt-1 text-xs text-gold-dark">Due September 20, 2026</p></Card><Card><p className="text-xs uppercase tracking-wide text-muted-foreground">Paid this semester</p><p className="mt-2 font-serif text-3xl font-bold text-navy">₱250.00</p><p className="mt-1 text-xs text-muted-foreground">1 transaction</p></Card><Card><p className="text-xs uppercase tracking-wide text-muted-foreground">Payment status</p><p className="mt-2 font-serif text-xl font-bold text-gold-dark">Partial payment</p><p className="mt-1 text-xs text-muted-foreground">Keep your account current</p></Card></div><Card><CardHeading title="Account ledger" action="Download statement" /><div className="flex flex-col divide-y divide-border">{payments.map(p => <div key={p.label} className="flex flex-col gap-2 py-4 first:pt-0 sm:flex-row sm:items-center sm:justify-between"><div><p className="text-sm font-bold text-navy">{p.label}</p><p className="mt-1 text-xs text-muted-foreground">Due {p.due}</p></div><div className="flex items-center gap-4"><span className={`text-sm font-bold ${p.status === 'Paid' ? 'text-gold-dark' : 'text-navy'}`}>{p.amount}</span><span className="rounded-full bg-accent px-3 py-1 text-[11px] font-semibold text-navy">{p.status}</span></div></div>)}</div></Card></> }

function Registrar({ setNotice }: { setNotice: (s: string) => void }) { return <><PageTitle eyebrow="Student services" title="Registrar Requests" description="Request official documents and follow their status from one place." action={<Button onClick={() => setNotice('New document request form opened in demo mode.')} className="bg-navy text-primary-foreground hover:bg-navy-light">New request <ArrowRight className="h-4 w-4" /></Button>} /><Card><CardHeading title="My requests" /><div className="flex flex-col divide-y divide-border">{registrarRequests.map(r => <div key={r.ref} className="flex flex-col gap-4 py-5 first:pt-0 sm:flex-row sm:items-center sm:justify-between"><div className="flex items-start gap-4"><div className="flex h-10 w-10 shrink-0 items-center justify-center rounded-lg bg-accent"><FileText className="h-5 w-5 text-gold-dark" /></div><div><p className="text-sm font-bold text-navy">{r.type}</p><p className="mt-1 text-xs text-muted-foreground">{r.ref} · Requested {r.date}</p></div></div><span className="w-fit rounded-full bg-accent px-3 py-1.5 text-xs font-bold text-navy">{r.status}</span></div>)}</div></Card><div className="mt-5 grid gap-4 sm:grid-cols-3">{['Certificate of Enrollment', 'Transcript of Records', 'Good Moral Certificate'].map(name => <button key={name} onClick={() => setNotice(`${name} request started in demo mode.`)} className="rounded-xl border border-dashed border-border bg-background p-5 text-left hover:border-gold hover:bg-accent"><FileText className="mb-4 h-5 w-5 text-gold-dark" /><p className="text-sm font-bold text-navy">{name}</p><p className="mt-1 text-xs text-muted-foreground">Start a new request</p></button>)}</div></> }

function buildCurriculumDownload(prog: typeof registrarCurriculums[number], filename: string) {
  const html = `<!DOCTYPE html><html><head><meta charset="utf-8"><title>${prog.code} - ${prog.title}</title><style>@page{size:A4 portrait;margin:14mm 12mm}body{font-family:Arial,Helvetica,sans-serif;color:#111;font-size:10pt;line-height:1.45;margin:0;padding:20px}h1{font-size:16pt;text-align:center;margin:0 0 4px;font-weight:700;text-transform:uppercase;letter-spacing:.02em} .sub{font-size:8pt;text-align:center;color:#555;margin-bottom:12px} table{width:100%;border-collapse:collapse;margin-top:10px} th,td{border:0.5pt solid #9ca3af;padding:4px 6px;font-size:8.5pt} th{background:#f3f4f6;text-transform:uppercase;font-size:7.5pt} .sem{font-weight:700;margin-top:14px;font-size:9pt} .footer{margin-top:18px;text-align:center;font-size:7pt;color:#666;border-top:0.5pt solid #ccc;padding-top:8px} .logo{display:block;margin:0 auto 10px;height:56px}</style></head><body><div style="text-align:center"><img class="logo" src="/images/cdl-logo.jpg" alt="Colegio De Loreto seal" style="height:56px" /><div style="font-size:8pt;letter-spacing:.18em;text-transform:uppercase;color:#555">Colegio De Loreto · Loreto, Agusan del Sur</div></div><h1>${prog.title}</h1><div class="sub">${prog.code} · ${prog.cmo} · AY ${prog.schoolYear} · ${prog.totalUnits} Units — Scientia ad Excellentiam</div>${prog.semesters.map(sem => `<div class="sem">${sem.yearLevel} · ${sem.semester}</div><table><thead><tr><th>Code</th><th>Description</th><th>Lec</th><th>Lab</th><th>Units</th><th>Prerequisite</th></tr></thead><tbody>${sem.subjects.map(s => `<tr><td><strong>${s.code}</strong></td><td>${s.description}</td><td style="text-align:center">${s.lec}</td><td style="text-align:center">${s.lab}</td><td style="text-align:center"><strong>${s.totalUnits}</strong></td><td>${s.prerequisite}</td></tr>`).join('')}</tbody></table>`).join('')}<div class="footer">Scientia ad Excellentiam · Office of the Registrar · Generated ${new Date().toLocaleDateString()}</div></body></html>`;
  const blob = new Blob([html], { type: 'text/html' });
  const url = URL.createObjectURL(blob);
  const a = document.createElement('a');
  a.href = url;
  a.download = filename.replace('.docx', '.html');
  document.body.appendChild(a);
  a.click();
  a.remove();
  URL.revokeObjectURL(url);
}

function StudentCurriculum({ setNotice }: { setNotice: (s: string) => void }) {
  const prog = registrarCurriculums.find(c => c.code === student.programCode) || registrarCurriculums[0];
  if (!prog) return <Card><p className="text-sm text-muted-foreground">No curriculum found for your program.</p></Card>;
  const handleDownload = () => {
    buildCurriculumDownload(prog, prog.docxFileName);
    setNotice(`Downloaded ${prog.code} curriculum — portrait, minimalist file.`);
  };
  return <div className="curriculum-print space-y-4">
    {/* Minimalist heading — enrolled program is the file heading, logo included in print/download */}
    <div className="print:block">
      <div className="hidden print:block text-center border-b border-slate-300 pb-3 mb-4">
        <div className="flex flex-col items-center">
          <Image src="/images/cdl-logo.jpg" alt="Colegio De Loreto seal" width={48} height={48} className="h-12 w-auto object-contain mb-2" />
          <p className="text-[8pt] uppercase tracking-[0.18em] text-slate-500">Colegio De Loreto · Loreto, Agusan del Sur</p>
          <h1 className="font-serif text-[14pt] font-bold uppercase tracking-wide text-black mt-1">{prog.title}</h1>
          <p className="text-[7.5pt] text-slate-600 mt-1">{prog.code} · {prog.cmo} · AY {prog.schoolYear} · {prog.totalUnits} Units</p>
        </div>
      </div>
      {/* Screen minimalist heading */}
      <div className="print:hidden rounded-xl border border-border bg-background p-5">
        <p className="text-[10px] font-bold uppercase tracking-[0.18em] text-gold-dark">Colegio De Loreto · Enrolled program</p>
        <h1 className="font-serif text-2xl font-bold uppercase tracking-wide text-navy mt-1">{prog.title}</h1>
        <p className="mt-1 text-xs text-muted-foreground">{prog.code} · {prog.cmo} · AY {prog.schoolYear} · {prog.totalUnits} Units · {student.program} — {student.id}</p>
        <div className="mt-3 flex flex-wrap gap-2">
          <Button onClick={() => window.print()} variant="outline" className="h-8 gap-2 border-navy text-navy text-xs"><Printer className="h-3.5 w-3.5" /> Print</Button>
          <Button onClick={handleDownload} variant="outline" className="h-8 gap-2 border-navy text-navy text-xs"><Download className="h-3.5 w-3.5" /> Download</Button>
        </div>
      </div>
      {/* Print-only action row hidden on screen but kept minimal */}
      <div className="hidden print:flex justify-end gap-2 text-[7pt] text-slate-500 mb-2"><span>Portrait · Minimal</span></div>
    </div>
    <div className="space-y-5 print:space-y-3">{prog.semesters.map((sem, sIdx) => <div key={sIdx} className="overflow-hidden rounded-xl border border-border bg-background shadow-sm print:rounded-none print:shadow-none print:break-inside-avoid print:border-slate-300"><div className="flex items-center justify-between border-b border-border bg-muted/40 px-5 py-3 print:bg-slate-100 print:py-2"><h3 className="font-serif text-sm font-bold text-navy print:text-black print:text-[9pt]">{sem.yearLevel} · {sem.semester}</h3><span className="text-xs font-semibold text-muted-foreground print:text-[7.5pt]">{sem.subjects.length} subjects</span></div><div className="overflow-x-auto"><table className="w-full text-left text-xs print:text-[8pt]"><thead className="bg-muted/20 text-muted-foreground uppercase tracking-wider font-bold print:bg-slate-100"><tr><th className="px-4 py-2">Code</th><th className="px-4 py-2">Description</th><th className="px-3 py-2 text-center">Lec</th><th className="px-3 py-2 text-center">Lab</th><th className="px-3 py-2 text-center">Units</th><th className="px-3 py-2">Prerequisite</th></tr></thead><tbody className="divide-y divide-border">{sem.subjects.map((sub, idx) => <tr key={idx} className="hover:bg-accent/30 print:hover:bg-white"><td className="px-4 py-2 font-bold text-navy print:text-black">{sub.code}</td><td className="px-4 py-2 text-foreground">{sub.description}</td><td className="px-3 py-2 text-center text-muted-foreground">{sub.lec}</td><td className="px-3 py-2 text-center text-muted-foreground">{sub.lab}</td><td className="px-3 py-2 text-center font-bold text-navy print:text-black">{sub.totalUnits}</td><td className="px-3 py-2 text-muted-foreground text-[11px] print:text-[7.5pt]">{sub.prerequisite}</td></tr>)}</tbody></table></div></div>)}</div>
  </div>
}

function LibraryView({ setNotice }: { setNotice: (s: string) => void }) { return <><PageTitle eyebrow="Campus resources" title="Library" description="Manage your borrowed materials and discover resources for your coursework." action={<Button onClick={() => setNotice('Library search is ready in demo mode.')} variant="outline" className="border-navy text-navy"><Search className="h-4 w-4" /> Search catalog</Button>} /><div className="grid gap-5 md:grid-cols-3"><Card className="md:col-span-2"><CardHeading title="Borrowed items" action="View history" /><div className="flex flex-col divide-y divide-border">{libraryItems.map(item => <div key={item.title} className="flex gap-4 py-4 first:pt-0"><div className="flex h-11 w-11 shrink-0 items-center justify-center rounded-lg bg-navy text-primary-foreground"><BookOpen className="h-5 w-5" /></div><div className="min-w-0 flex-1"><p className="text-sm font-bold text-navy">{item.title}</p><p className="mt-1 text-xs text-muted-foreground">{item.author} · {item.type}</p><p className="mt-2 text-xs font-semibold text-gold-dark">Due {(item as any).due ?? '—'}</p></div><button onClick={() => setNotice('Renewal requested in demo mode.')} className="h-fit text-xs font-bold text-navy hover:text-gold-dark">Renew</button></div>)}</div></Card><Card className="bg-navy text-primary-foreground"><Library className="mb-5 h-7 w-7 text-gold" /><h3 className="font-serif text-xl font-bold">Library hours</h3><div className="mt-5 flex flex-col gap-3 text-sm text-primary-foreground/70"><div className="flex justify-between"><span>Monday – Friday</span><span className="font-semibold text-gold">7 AM – 7 PM</span></div><div className="flex justify-between"><span>Saturday</span><span className="font-semibold text-gold">8 AM – 5 PM</span></div><div className="flex justify-between"><span>Sunday</span><span className="font-semibold text-gold">Closed</span></div></div></Card></div></> }

function Support({ setNotice }: { setNotice: (s: string) => void }) { return <><PageTitle eyebrow="We&apos;re here to help" title="Help & Support" description="Find answers, contact the right office, or send a concern to the student services team." /><div className="grid gap-5 md:grid-cols-3">{[{ icon: UserRound, title: 'Student Affairs', detail: 'For campus life, organizations, and student concerns.' }, { icon: BookOpen, title: 'Academic advising', detail: 'For course planning, program questions, and advising.' }, { icon: CreditCard, title: 'Finance office', detail: 'For tuition, payment, and account balance questions.' }].map(item => <Card key={item.title}><item.icon className="mb-5 h-6 w-6 text-gold-dark" /><h3 className="font-serif text-lg font-bold text-navy">{item.title}</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">{item.detail}</p><button onClick={() => setNotice(`Message sent to ${item.title} in demo mode.`)} className="mt-5 text-xs font-bold text-navy hover:text-gold-dark">Contact office <ArrowRight className="ml-1 inline h-3 w-3" /></button></Card>)}</div><Card className="mt-5"><CardHeading title="Send a message" /><div className="grid gap-4 md:grid-cols-2"><label className="flex flex-col gap-2 text-sm font-semibold text-navy">Topic<select className="h-11 rounded-lg border border-border bg-background px-3 font-normal"><option>General inquiry</option><option>Academic concern</option><option>Technical support</option></select></label><label className="flex flex-col gap-2 text-sm font-semibold text-navy">Subject<input placeholder="How can we help?" className="h-11 rounded-lg border border-border px-3 font-normal outline-none focus:ring-2 focus:ring-gold" /></label><label className="flex flex-col gap-2 text-sm font-semibold text-navy md:col-span-2">Message<textarea rows={4} placeholder="Write your message here..." className="rounded-lg border border-border p-3 font-normal outline-none focus:ring-2 focus:ring-gold" /></label></div><Button onClick={() => setNotice('Your message has been sent in demo mode.')} className="mt-5 bg-navy text-primary-foreground hover:bg-navy-light"><Send className="h-4 w-4" /> Send message</Button></Card></> }
