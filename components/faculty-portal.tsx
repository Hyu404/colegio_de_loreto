'use client'

import { useState } from 'react'
import { ArrowRight, Bell, BookOpen, CalendarDays, Check, ChevronRight, CircleHelp, ClipboardCheck, LayoutDashboard, LockKeyhole, LogOut, Mail, Megaphone, Menu, UserRound, Users, X } from 'lucide-react'
import { Button } from '@/components/ui/button'
import { faculty, facultyAnnouncements, facultyClasses, facultyNavItems, facultySchedule, portalStats, roster, type FacultySection } from '@/lib/faculty-data'

const icons = { LayoutDashboard, BookOpen, Users, ClipboardCheck, CalendarDays, Megaphone, CircleHelp }

export function FacultyPortal() {
  const [signedIn, setSignedIn] = useState(false); const [section, setSection] = useState<FacultySection>('Overview'); const [mobileOpen, setMobileOpen] = useState(false); const [notice, setNotice] = useState(''); const [search, setSearch] = useState('')
  if (!signedIn) return <section className="flex min-h-screen items-center justify-center bg-navy-dark px-4 py-12"><div className="w-full max-w-md bg-background p-8 shadow-2xl md:p-10"><div className="mb-8 flex items-center gap-3"><div className="flex h-12 w-12 items-center justify-center bg-navy text-lg font-bold text-gold">CDL</div><div><p className="font-serif text-lg font-bold text-navy">Colegio De Loreto</p><p className="text-xs uppercase tracking-wider text-muted-foreground">Faculty portal</p></div></div><p className="mb-2 text-sm font-bold uppercase tracking-[0.18em] text-gold-dark">Welcome back</p><h1 className="font-serif text-3xl font-bold text-navy">Sign in to your workspace</h1><p className="mt-3 text-sm leading-6 text-muted-foreground">Access your classes, rosters, gradebook, and faculty resources.</p><label className="mt-8 block text-sm font-semibold text-foreground">Faculty ID<input defaultValue="FAC-0047" className="mt-2 h-11 w-full rounded-md border border-border bg-background px-3 outline-none focus:ring-2 focus:ring-gold" /></label><label className="mt-4 block text-sm font-semibold text-foreground">Password<input type="password" defaultValue="demo" className="mt-2 h-11 w-full rounded-md border border-border bg-background px-3 outline-none focus:ring-2 focus:ring-gold" /></label><Button onClick={() => setSignedIn(true)} className="mt-6 h-11 w-full bg-gold font-bold text-navy-dark hover:bg-gold-dark">Sign in <ArrowRight className="ml-2 h-4 w-4" /></Button><p className="mt-6 flex items-center justify-center gap-2 text-xs text-muted-foreground"><LockKeyhole className="h-3.5 w-3.5" /> Demo access · Use any credentials</p><a href="/" className="mt-8 block text-center text-sm font-semibold text-navy hover:underline">Return to public website</a></div></section>
  const navigate = (item: FacultySection) => { setSection(item); setMobileOpen(false); setNotice('') }
  const filteredRoster = roster.filter((s) => `${s.name} ${s.id} ${s.program}`.toLowerCase().includes(search.toLowerCase()))
  return <div className="min-h-screen bg-muted/30 text-foreground"><aside className={`fixed inset-y-0 left-0 z-40 flex w-72 flex-col bg-navy-dark text-primary-foreground transition-transform lg:translate-x-0 ${mobileOpen ? 'translate-x-0' : '-translate-x-full'}`}><div className="flex h-20 items-center justify-between border-b border-primary-foreground/10 px-6"><div className="flex items-center gap-3"><div className="flex h-10 w-10 items-center justify-center bg-gold text-sm font-black text-navy-dark">CDL</div><div><p className="font-serif font-bold">Faculty portal</p><p className="text-[10px] uppercase tracking-wider text-primary-foreground/50">Academic workspace</p></div></div><button className="lg:hidden" onClick={() => setMobileOpen(false)} aria-label="Close menu"><X className="h-5 w-5" /></button></div><nav className="flex-1 space-y-1 px-4 py-6">{facultyNavItems.map((item) => { const Icon = icons[item.icon as keyof typeof icons]; return <button key={item.label} onClick={() => navigate(item.label)} className={`flex w-full items-center gap-3 px-3 py-3 text-left text-sm font-semibold transition-colors ${section === item.label ? 'bg-gold text-navy-dark' : 'text-primary-foreground/70 hover:bg-primary-foreground/10 hover:text-primary-foreground'}`}><Icon className="h-4 w-4" />{item.label}</button> })}</nav><div className="border-t border-primary-foreground/10 p-4"><div className="mb-4 flex items-center gap-3"><div className="flex h-9 w-9 items-center justify-center rounded-full bg-gold text-xs font-bold text-navy-dark">ER</div><div><p className="text-sm font-semibold">{faculty.name}</p><p className="text-xs text-primary-foreground/50">{faculty.id}</p></div></div><button onClick={() => setSignedIn(false)} className="flex w-full items-center gap-2 px-3 py-2 text-sm text-primary-foreground/60 hover:text-gold"><LogOut className="h-4 w-4" /> Sign out</button></div></aside><div className="lg:pl-72"><header className="sticky top-0 z-30 flex h-20 items-center justify-between border-b border-border bg-background/95 px-4 backdrop-blur md:px-8"><div className="flex items-center gap-3"><button onClick={() => setMobileOpen(true)} className="rounded-md p-2 text-navy lg:hidden" aria-label="Open menu"><Menu className="h-5 w-5" /></button><div><p className="text-xs font-bold uppercase tracking-[0.16em] text-gold-dark">Faculty workspace</p><h1 className="font-serif text-xl font-bold text-navy">{section}</h1></div></div><button onClick={() => setNotice('You have 3 new updates.')} className="relative rounded-md p-2 text-navy hover:bg-accent" aria-label="Notifications"><Bell className="h-5 w-5" /><span className="absolute right-1 top-1 h-2 w-2 rounded-full bg-gold" /></button></header><main className="mx-auto max-w-7xl px-4 py-8 md:px-8">{notice && <div className="mb-6 flex items-center justify-between bg-navy px-4 py-3 text-sm text-primary-foreground"><span>{notice}</span><button onClick={() => setNotice('')} aria-label="Dismiss notification"><X className="h-4 w-4" /></button></div>}{section === 'Overview' && <Overview onNavigate={navigate} />}{section === 'My Classes' && <Classes />}{section === 'Students' && <Students search={search} setSearch={setSearch} filtered={filteredRoster} />}{section === 'Gradebook' && <Gradebook onNotice={() => setNotice('Grades saved successfully for AGRI 101.')} />}{section === 'Schedule' && <Schedule />}{section === 'Announcements' && <Announcements onNotice={() => setNotice('Announcement draft saved.')} />}{section === 'Help & Support' && <Support onNotice={() => setNotice('Support request submitted.')} />}</main></div></div>
}

function Overview({ onNavigate }: { onNavigate: (s: FacultySection) => void }) { return <><div className="mb-8"><p className="text-sm text-muted-foreground">Tuesday, September 8, 2026</p><h2 className="mt-1 font-serif text-3xl font-bold text-navy">Good morning, Dr. Ramos.</h2><p className="mt-2 text-muted-foreground">Here is your academic workspace at a glance.</p></div><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{portalStats.map((s) => <div key={s.label} className="border border-border bg-background p-5"><p className="text-sm text-muted-foreground">{s.label}</p><p className="mt-2 font-serif text-3xl font-bold text-navy">{s.value}</p><p className="mt-1 text-xs text-muted-foreground">{s.note}</p></div>)}</div><div className="mt-8 grid gap-6 xl:grid-cols-[1.4fr_1fr]"><div className="border border-border bg-background p-6"><div className="flex items-center justify-between"><h3 className="font-serif text-xl font-bold text-navy">Today&apos;s classes</h3><button onClick={() => onNavigate('Schedule')} className="text-sm font-semibold text-gold-dark hover:underline">View schedule</button></div><div className="mt-5 space-y-3">{facultySchedule[0].items.map((item) => <div key={item.time} className="flex items-center justify-between border-l-4 border-gold bg-muted/50 p-4"><div><p className="font-semibold text-navy">{item.code}</p><p className="text-sm text-muted-foreground">{item.time}</p></div><span className="text-xs text-muted-foreground">{item.room}</span></div>)}</div></div><div className="border border-border bg-background p-6"><h3 className="font-serif text-xl font-bold text-navy">Pending attention</h3><div className="mt-5 space-y-4">{[{ label: 'Grade submissions', count: '20 pending', target: 'Gradebook' }, { label: 'Active classes', count: '3 sections', target: 'My Classes' }, { label: 'New announcements', count: '3 updates', target: 'Announcements' }].map((item) => <button key={item.label} onClick={() => onNavigate(item.target as FacultySection)} className="flex w-full items-center justify-between border-b border-border pb-4 text-left last:border-0"><span><span className="block text-sm font-semibold">{item.label}</span><span className="text-xs text-muted-foreground">{item.count}</span></span><ChevronRight className="h-4 w-4 text-gold-dark" /></button>)}</div></div></div></> }

function Classes() { return <div><h2 className="font-serif text-3xl font-bold text-navy">My classes</h2><p className="mt-2 text-muted-foreground">Your teaching assignments for 1st Semester, AY 2026–2027.</p><div className="mt-6 grid gap-5 lg:grid-cols-3">{facultyClasses.map((c) => <article key={c.code} className="border border-border bg-background p-6"><span className="text-xs font-bold uppercase tracking-wider text-gold-dark">{c.code}</span><h3 className="mt-3 font-serif text-xl font-bold text-navy">{c.name}</h3><div className="mt-5 space-y-2 text-sm text-muted-foreground"><p>{c.schedule}</p><p>{c.room}</p><p>{c.students} enrolled</p></div><Button className="mt-6 w-full bg-navy text-primary-foreground hover:bg-navy-light">Open class <ArrowRight className="ml-2 h-4 w-4" /></Button></article>)}</div></div> }

function Students({ search, setSearch, filtered }: { search: string; setSearch: (v: string) => void; filtered: typeof roster }) { return <div><h2 className="font-serif text-3xl font-bold text-navy">Students</h2><p className="mt-2 text-muted-foreground">View student rosters and academic standing across your classes.</p><input value={search} onChange={(e) => setSearch(e.target.value)} placeholder="Search students" className="mt-6 h-11 w-full max-w-md rounded-md border border-border bg-background px-3 text-sm outline-none focus:ring-2 focus:ring-gold" /><div className="mt-6 overflow-x-auto border border-border bg-background"><table className="w-full min-w-[680px] text-left text-sm"><thead className="bg-navy text-primary-foreground"><tr><th className="p-4 font-semibold">Student</th><th className="p-4 font-semibold">Program</th><th className="p-4 font-semibold">Attendance</th><th className="p-4 font-semibold">Current grade</th></tr></thead><tbody>{filtered.map((s) => <tr key={s.id} className="border-b border-border last:border-0"><td className="p-4"><p className="font-semibold text-navy">{s.name}</p><p className="text-xs text-muted-foreground">{s.id}</p></td><td className="p-4 text-muted-foreground">{s.program}</td><td className="p-4">{s.attendance}</td><td className="p-4 font-semibold text-gold-dark">{s.grade}</td></tr>)}</tbody></table></div></div> }

function Gradebook({ onNotice }: { onNotice: () => void }) { return <div><h2 className="font-serif text-3xl font-bold text-navy">Gradebook</h2><p className="mt-2 text-muted-foreground">AGRI 101 · Introduction to Agricultural Business</p><div className="mt-6 border border-border bg-background p-6"><div className="flex flex-wrap items-center justify-between gap-3 border-b border-border pb-5"><div><p className="font-semibold text-navy">Final grades</p><p className="text-sm text-muted-foreground">20 of 32 grades still require submission.</p></div><Button onClick={onNotice} className="bg-gold text-navy-dark hover:bg-gold-dark"><Check className="mr-2 h-4 w-4" />Save grades</Button></div><div className="mt-5 space-y-3">{roster.map((s) => <div key={s.id} className="flex flex-col gap-3 border-b border-border pb-3 sm:flex-row sm:items-center sm:justify-between"><div><p className="font-semibold text-navy">{s.name}</p><p className="text-xs text-muted-foreground">{s.id}</p></div><select defaultValue={s.grade} className="h-9 rounded-md border border-border bg-background px-3 text-sm"><option>1.00</option><option>1.25</option><option>1.50</option><option>1.75</option><option>2.00</option><option>2.25</option></select></div>)}</div></div></div> }

function Schedule() { return <div><h2 className="font-serif text-3xl font-bold text-navy">Weekly schedule</h2><p className="mt-2 text-muted-foreground">September 9–13, 2026 · 1st Semester</p><div className="mt-6 grid gap-3 md:grid-cols-5">{facultySchedule.map((day) => <div key={day.day} className="border border-border bg-background"><div className="border-b border-border bg-navy p-4 text-primary-foreground"><p className="text-sm font-bold">{day.day}</p><p className="text-xs text-primary-foreground/60">September {day.date}</p></div><div className="space-y-3 p-3">{day.items.map((item) => <div key={item.time} className="border-l-2 border-gold bg-muted/50 p-3"><p className="text-xs font-bold text-gold-dark">{item.time}</p><p className="mt-1 text-sm font-semibold text-navy">{item.code}</p><p className="mt-1 text-xs text-muted-foreground">{item.room}</p></div>)}</div></div>)}</div></div> }

function Announcements({ onNotice }: { onNotice: () => void }) { return <div><h2 className="font-serif text-3xl font-bold text-navy">Announcements</h2><p className="mt-2 text-muted-foreground">Stay connected with campus updates and faculty notices.</p><div className="mt-6 grid gap-6 lg:grid-cols-[1fr_360px]"><div className="space-y-3">{facultyAnnouncements.map((a) => <article key={a.title} className="flex gap-4 border border-border bg-background p-5"><div className="w-14 shrink-0 text-center"><p className="text-xs font-bold text-gold-dark">{a.date.split(' ')[0]}</p><p className="font-serif text-xl font-bold text-navy">{a.date.split(' ')[1]}</p></div><div><p className="text-xs font-bold uppercase tracking-wider text-muted-foreground">{a.tag}</p><h3 className="mt-1 font-semibold text-navy">{a.title}</h3></div></article>)}</div><div className="border border-border bg-background p-6"><h3 className="font-serif text-xl font-bold text-navy">Create announcement</h3><input placeholder="Title" className="mt-5 h-10 w-full rounded-md border border-border px-3 text-sm" /><textarea placeholder="Write a message for your students..." className="mt-3 min-h-28 w-full rounded-md border border-border p-3 text-sm" /><Button onClick={onNotice} className="mt-3 w-full bg-navy text-primary-foreground hover:bg-navy-light">Save draft</Button></div></div></div> }

function Support({ onNotice }: { onNotice: () => void }) { return <div><h2 className="font-serif text-3xl font-bold text-navy">Help & support</h2><p className="mt-2 text-muted-foreground">Need help with the faculty portal?</p><div className="mt-6 grid gap-5 md:grid-cols-3">{[{ icon: BookOpen, title: 'Faculty handbook', text: 'Policies, procedures, and academic guidelines.' }, { icon: Mail, title: 'Contact IT support', text: 'Get help with access and technical issues.' }, { icon: CircleHelp, title: 'Submit a request', text: 'Send a concern to the faculty office.' }].map((item) => <button key={item.title} onClick={onNotice} className="border border-border bg-background p-6 text-left hover:border-gold"><item.icon className="h-6 w-6 text-gold-dark" /><h3 className="mt-4 font-serif text-xl font-bold text-navy">{item.title}</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">{item.text}</p></button>)}</div></div> }
