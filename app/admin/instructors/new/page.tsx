'use client'

import Link from 'next/link'
import { ArrowLeft, Check, UserPlus } from 'lucide-react'
import { useState } from 'react'
import { registrarRooms, registrarSections, registrarSubjects } from '@/lib/registrar-data'

const inputClass = 'h-11 w-full rounded-lg border border-border bg-background px-3 text-sm outline-none ring-gold focus:ring-2'
const labelClass = 'flex flex-col gap-2 text-sm font-semibold text-navy'

export default function AddInstructorPage() {
  const [saved, setSaved] = useState(false)
  function submit(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); setSaved(true) }
  return <main className="min-h-screen bg-muted/40 px-4 py-8 text-foreground sm:px-6 lg:px-10"><div className="mx-auto max-w-5xl"><Link href="/admin" className="inline-flex items-center gap-2 text-sm font-semibold text-navy hover:text-gold-dark"><ArrowLeft className="h-4 w-4" /> Back to registrar</Link><div className="mt-7 flex items-start gap-4"><div className="flex h-12 w-12 shrink-0 items-center justify-center rounded-xl bg-gold text-navy-dark"><UserPlus className="h-6 w-6" /></div><div><p className="text-xs font-bold uppercase tracking-[.2em] text-gold-dark">Registrar management · Demo form</p><h1 className="mt-1 font-serif text-3xl font-bold text-navy">Add instructor</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-muted-foreground">Create a faculty record and assign subject loads, rooms, sections, and schedules.</p></div></div>{saved ? <div className="mt-8 rounded-2xl border border-gold/50 bg-accent p-8 text-center"><div className="mx-auto flex h-12 w-12 items-center justify-center rounded-full bg-gold text-navy-dark"><Check className="h-6 w-6" /></div><h2 className="mt-4 font-serif text-2xl font-bold text-navy">Instructor record staged</h2><p className="mx-auto mt-2 max-w-md text-sm leading-6 text-muted-foreground">Demo mode saved this record for review. Connect the registrar database later to persist it permanently.</p><button onClick={() => setSaved(false)} className="mt-5 rounded-lg bg-navy px-4 py-2.5 text-sm font-bold text-primary-foreground">Add another instructor</button></div> : <form onSubmit={submit} className="mt-8 grid gap-5 lg:grid-cols-[1fr_320px]"><div className="rounded-2xl border border-border bg-background p-6 shadow-sm"><h2 className="font-serif text-xl font-bold text-navy">Faculty information</h2><p className="mt-1 text-xs text-muted-foreground">Identity, department, and academic credentials.</p><div className="mt-6 grid gap-5 sm:grid-cols-2"><label className={labelClass}>First name<input required className={inputClass} placeholder="Elena" /></label><label className={labelClass}>Last name<input required className={inputClass} placeholder="Ramos" /></label><label className={labelClass}>Faculty ID<input required className={inputClass} placeholder="FAC-004" /></label><label className={labelClass}>Email address<input required type="email" className={inputClass} placeholder="faculty@email.com" /></label><label className={labelClass}>Department<input required className={inputClass} placeholder="Agribusiness" /></label><label className={labelClass}>Specialty<input className={inputClass} placeholder="Agricultural Economics" /></label></div><div className="mt-8 border-t border-border pt-6"><h2 className="font-serif text-xl font-bold text-navy">Educational attainment</h2><div className="mt-5 grid gap-5 sm:grid-cols-3"><label className={labelClass}>Bachelor&apos;s degree<input required className={inputClass} placeholder="BS Agricultural Business" /></label><label className={labelClass}>Master&apos;s degree<input className={inputClass} placeholder="MBA or N/A" /></label><label className={labelClass}>Doctorate<input className={inputClass} placeholder="PhD or N/A" /></label></div></div></div><div className="flex h-fit flex-col gap-5"><div className="rounded-2xl border border-border bg-background p-6 shadow-sm"><h2 className="font-serif text-xl font-bold text-navy">Subject load</h2><p className="mt-1 text-xs leading-5 text-muted-foreground">Assign the instructor&apos;s first teaching load.</p><div className="mt-5 flex flex-col gap-5"><label className={labelClass}>Subject<select required className={inputClass} defaultValue=""><option value="" disabled>Select subject</option>{registrarSubjects.map(x => <option key={x.code}>{x.code} · {x.name}</option>)}</select></label><label className={labelClass}>Section<select required className={inputClass} defaultValue=""><option value="" disabled>Select section</option>{registrarSections.map(x => <option key={x.name}>{x.name}</option>)}</select></label><label className={labelClass}>Room<select required className={inputClass} defaultValue=""><option value="" disabled>Select room</option>{registrarRooms.map(x => <option key={x}>{x}</option>)}</select></label><label className={labelClass}>Schedule<input required className={inputClass} placeholder="MWF · 8:00–9:00 AM" /></label></div></div><div className="rounded-xl border border-gold/40 bg-accent p-4 text-xs leading-5 text-muted-foreground"><span className="font-bold text-navy">Scheduling reminder:</span> Check room and section conflicts before publishing the load.</div><button className="h-11 rounded-lg bg-gold px-4 text-sm font-bold text-navy-dark transition hover:bg-gold-dark">Save instructor record <Check className="ml-2 inline h-4 w-4" /></button></div></form>}</div></main>
}
